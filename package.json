{
  "scripts": {
    "dev:css": "npx tailwindcss -i input.css -o assets/css/style.css -w",
    "dev:hugo": "hugo server",
    "dev": "run-p dev:*",
    "build:css": "npx tailwindcss -i input.css -o assets/css/style.css",
    "build:minifyCss": "hugo --minify",
    "build:hugo": "hugo",
    "build": "run-s build:*"
  },
  "devDependencies": {
    "@tailwindcss/aspect-ratio": "^0.4.2",
    "@tailwindcss/typography": "^0.5.9",
    "tailwindcss": "^3.3.3"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "tailwind-scrollbar": "^2.1.0"
  }
}
